name: Build and Test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Building Application"

  test:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - run: echo "Testing Application"
