name: Include Exclude example

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    strategy:
      max-parallel: 2
      matrix:
        role: [dev, qa]

        include:
          - role: prod

        exclude:
          - role: qa

    steps:
      - run: echo "Role is ${{ matrix.role}}"
